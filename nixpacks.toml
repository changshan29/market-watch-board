[phases.setup]
nixPkgs = ["nodejs-18_x", "python310", "chromium", "chromedriver"]

[phases.install]
cmds = [
  "python3 -m venv /opt/venv",
  ". /opt/venv/bin/activate && pip install -r requirements.txt"
]

[start]
cmd = ". /opt/venv/bin/activate && node server.js"
